Event Management System is a full-stack web application that allows users to create, update, and manage events easily.
It includes user authentication (signup, login, logout), CRUD operations, and real-time seat availability tracking. 
The UI is built using HTML, CSS, and JavaScript with interactive features like read more/see less. 
The backend is integrated with MongoDB (NoSQL), with data managed using MongoDB Compass.
