# Event-Management-system
Event Management System built with frontend and backend integration. Features include user authentication (login/signup/logout), CRUD operations, real-time seat availability monitoring, and interactive UI elements like read more/less. Uses MongoDB (NoSQL) with data managed via MongoDB Compass.
